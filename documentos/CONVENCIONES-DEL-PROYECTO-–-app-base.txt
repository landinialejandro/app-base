CONVENCIONES-DEL-PROYECTO – app-base

Objetivo del documento

Este documento define las convenciones estructurales y de desarrollo del proyecto app-base.

Su objetivo es mantener consistencia técnica, previsibilidad arquitectónica y coherencia de código a medida que el sistema crece.

Las convenciones aquí definidas deben respetarse al crear:

nuevos módulos

nuevas entidades

nuevas vistas

nuevos controladores

nuevos flujos operativos

Convenciones generales

El proyecto sigue los principios de:

simplicidad

claridad estructural

baja duplicación

coherencia entre módulos

previsibilidad del comportamiento del sistema

Cuando existan varias alternativas técnicas, se debe priorizar siempre la opción más simple y consistente con el resto del proyecto.

Convenciones de nombres

Modelos

Los modelos deben estar en singular.

Ejemplos:

User
Tenant
Membership
Party
Project
Order
Document
Asset

Tablas

Las tablas deben utilizar plural en snake_case.

Ejemplos:

users
tenants
memberships
parties
projects
orders
documents
assets

Controladores

Los controladores utilizan el sufijo:

Controller

Ejemplos:

UserController
OrderController
DocumentController
AssetController

Rutas

Las rutas utilizan plural.

Ejemplos:

/users
/orders
/documents
/assets

Convención actual de declaración de rutas CRUD

Como convención actual del proyecto, las rutas CRUD del core funcional deben declararse explícitamente en routes/web.php.

No se utilizará Route::resource(...) mientras el proyecto mantenga compatibilidad objetivo con hosting compartido básico sin CLI, salvo revisión técnica posterior en un entorno más robusto.

Esta decisión busca priorizar previsibilidad operativa y reducir riesgo en despliegues cerrados preparados en local.

Convenciones de multi-tenancy

Las entidades del negocio deben incluir siempre:

tenant_id

El tenant determina el aislamiento lógico de los datos dentro del sistema.

Las entidades del sistema no deben incluir tenant_id.

Ejemplos de entidades del sistema:

User
Tenant
Membership

Acceso operativo por tenant

La restricción operativa de acceso a empresa debe validarse por memberships.status = active.

No debe utilizarse desactivación global de users para resolver bajas, bloqueos o pérdida de acceso específicos de un tenant.

Trait TenantScoped

Las entidades del negocio deben utilizar el trait:

TenantScoped

Este trait se encarga de:

filtrar automáticamente por tenant_id

asignar tenant_id al crear registros

Esto garantiza que los datos de diferentes empresas no se mezclen.

Uso de tenant_id en fillable

Como regla general, tenant_id no debe exponerse en formularios ni venir desde input del usuario.

El tenant siempre debe resolverse internamente mediante:

app('tenant')

y asignarse automáticamente por el sistema.

Algunos modelos padre del proyecto exponen tenant_id en fillable por consistencia histórica, pero igualmente no debe venir desde formularios públicos ni desde input del usuario.

En entidades hijas (por ejemplo items o registros dependientes), tenant_id tampoco debe exponerse desde formularios del usuario.

Sin embargo, si el registro hijo se crea internamente desde un flujo controlado del sistema y el tenant se hereda explícitamente desde la entidad padre, puede incluirse tenant_id en fillable para permitir la creación interna consistente mediante create().

Esto permite manejar correctamente casos como:

document_items creados desde documentos
order_items copiados hacia documentos derivados

Convenciones de entidades hijas

Las entidades hijas deben cumplir:

pertenecer a una cabecera

heredar tenant_id de la cabecera

no recibir tenant_id desde formularios del usuario

Ejemplos:

order_items
document_items

Convenciones de ownership funcional y permisos operativos simples

Cuando un módulo lo permita, puede adoptarse un criterio simple de ownership funcional del registro en lugar de una capa temprana de permisos compleja.

Reglas:

usuario normal gestiona los registros de su responsabilidad operativa

owner y admin pueden gestionar registros ajenos dentro del tenant

los borrados sensibles pueden reservarse a owner y admin aunque la edición o seguimiento sea más amplia

Este patrón ya se considera válido para tasks.

Convenciones de flujo Task → Order

Cuando una entidad derive otra en una relación operativa 1 a 1, el vínculo puede resolverse desde la entidad derivada.

En el caso actual:

orders.task_id expresa que una orden nace desde una tarea

Reglas:

una tarea puede derivar en una única orden

la relación debe mantenerse simple y explícita

no debe duplicarse el vínculo en ambas entidades si no aporta valor real

Convenciones de flujo Asset → Order

Un order puede vincularse opcionalmente a un asset.

Si un order tiene asset_id, debe ser consistente con party_id.

Si una orden ya quedó vinculada a un activo, no debe permitirse cambiar libremente asset_id ni party_id en edición si eso compromete la trazabilidad funcional del registro.

Convenciones de flujo Order → Document

Un order puede dar origen a múltiples documents.

La creación de documentos derivados desde una orden debe realizarse mediante flujos controlados del sistema, nunca mediante formularios manuales que permitan alterar la relación.

Cuando un documento se genera desde una orden:

documents hereda automáticamente:

tenant_id
party_id
order_id
asset_id si la orden lo posee

Los order_items pueden copiarse a document_items según el tipo documental.

Luego de copiar los ítems, los totales del documento deben recalcularse.

Convenciones para módulos agregadores con uso operativo diario

Cuando un módulo agregador del core tenga uso operativo diario, su index puede dejar de ser nominal y pasar a una tabla operativa con métricas derivadas.

Reglas:

si el módulo tiene estados cerrados, debe usar catálogo centralizado y badge visual

los porcentajes, contadores o métricas operativas deben resolverse preferentemente en controller, query u otra capa previa, no en Blade, cuando la lógica empiece a crecer

este patrón ya se considera válido para projects.index

Convenciones de perfil de empresa

El perfil de empresa es una pantalla reservada a owner.

La configuración básica de empresa debe persistirse en tenants.settings cuando no justifique entidad propia.

Si una configuración adquiere complejidad funcional significativa, podrá migrarse a entidad específica más adelante.

Convenciones de invitaciones

Las invitaciones de miembros del tenant se generan desde el contexto del owner.

No debe permitirse generar una invitación nueva si:

el email ya pertenece a un usuario del tenant

existe una invitación pendiente y vigente para ese email y tenant

El owner puede eliminar invitaciones pendientes no aceptadas.

Convenciones de búsquedas y filtros en index

Los listados index deben preferir filtros simples por GET.

Reglas:

usar when(...) en queries para mantener controladores legibles

usar paginate(...)->withQueryString() para preservar filtros entre páginas

usar q como búsqueda libre cuando aporte valor real

usar select para filtros cerrados o catálogos

evitar buscadores globales o sobrearquitectura antes de necesidad real

Los catálogos reutilizables del sistema también deben aprovecharse en filtros cuando corresponda.

Convenciones de controladores

Los controladores deben ser lo más delgados posible.

La lógica de negocio debe colocarse preferentemente en:

servicios
clases de soporte
modelos

Los controladores deben encargarse principalmente de:

recibir requests

validar datos

delegar lógica

devolver respuestas

Convenciones de catálogos

Cuando un módulo tenga estados cerrados o valores controlados, se debe crear un catálogo centralizado.

El catálogo debe concentrar:

constantes

labels

clases visuales (badgeClass)

listas permitidas

Esto evita lógica repetida en controladores o vistas.

Ejemplo de responsabilidades del catálogo:

definir estados válidos

devolver label del estado

devolver clase CSS del badge

Uso de catálogos en vistas

Las vistas Blade no deben resolver estados mediante lógica inline si el catálogo ya puede hacerlo.

En lugar de usar lógica condicional o match en la vista, debe utilizarse el catálogo correspondiente.

Uso recomendado:

<span class="status-badge {{ Catalog::badgeClass($value) }}"> {{ Catalog::label($value) }} </span>

Esto mantiene las vistas simples y evita duplicación de lógica.

Convenciones de Blade

Las vistas Blade deben mantenerse:

simples

legibles

sin lógica compleja

La lógica debe resolverse previamente en:

controladores
view models
catálogos
servicios

Cuando una vista crezca demasiado, debe dividirse en partials por bloques visuales claros.

Se deben preferir partials antes que componentes globales cuando el bloque todavía sea local a una sola pantalla.

Convenciones de formularios

Los formularios deben enviar únicamente:

datos propios de la entidad

No deben enviar:

tenant_id
user_id
campos derivados

Estos valores deben resolverse internamente en el backend.

Convenciones de frontend y comportamiento reutilizable

Evitar onclick, onsubmit, onchange y cualquier handler JavaScript inline en Blade.

El comportamiento reutilizable debe resolverse en:

public/js/app-base.js

La activación declarativa debe realizarse mediante:

data-action="app-..."

El comportamiento exclusivo y no reutilizable de una vista puede resolverse con:

@push('scripts')

en el blade raíz correspondiente.

No usar clases CSS para comportamiento.

Separación recomendada:

clases CSS = estilo
data-* = comportamiento

Preferir soluciones simples y declarativas antes que lógica distribuida en múltiples blades.

Convenciones de preservación de contexto UX

En vistas con tabs, filtros o acciones internas, debe preservarse el contexto funcional al refrescar o redirigir cuando eso mejore la UX.

Ejemplos de contexto a preservar:

tab activa
filtros
paginación cuando aplique

Convenciones de vocabulario UI

Nombre genérico visible de la entidad parties en interfaz:

Contacto

Cliente debe utilizarse solo como categoría, clase o tipo específico dentro del catálogo correspondiente, no como nombre genérico de la entidad.

Para asignación interna de tareas, el término visible recomendado es:

Colaborador

El verbo destructivo estándar en botones, confirmaciones y mensajes del sistema es:

Eliminar

Convenciones de acciones visuales

Fuera de tablas, los botones principales pueden usar icono + texto cuando eso refuerce la asociación visual de la acción.

Dentro de tablas o celdas compact-actions-cell, las acciones deben mostrarse solo con icono.

Todo botón de solo icono debe incluir:

title
aria-label

Convenciones de seguridad

El sistema debe evitar que un usuario pueda acceder a registros de otro tenant.

Esto se logra mediante:

TenantScoped
ResolvesTenantRouteBinding

Nunca se deben realizar consultas que ignoren el tenant actual.

Convenciones de flujo de invitaciones

Las invitaciones forman parte del flujo de onboarding y acceso al sistema.

Cuando una invitación corresponde a un usuario que ya posee cuenta en el sistema, el flujo debe:

redirigir al login si el usuario no está autenticado

una vez autenticado, reanudar el flujo original de la invitación

En estos casos no debe aplicarse la redirección estándar posterior al login.

El sistema debe retornar al flujo de aceptación de la invitación.

Esto garantiza continuidad del proceso de incorporación al tenant correspondiente.

Convención de simplicidad

Antes de introducir una nueva abstracción, evaluar si realmente es necesaria.

El sistema prioriza:

claridad

consistencia

bajo nivel de complejidad accidental

Las soluciones simples y previsibles deben preferirse sobre arquitecturas excesivamente abstractas.