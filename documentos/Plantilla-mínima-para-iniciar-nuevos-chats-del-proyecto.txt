PLANTILLA MÍNIMA PARA INICIAR NUEVOS CHATS DEL PROYECTO – app-base

PROYECTO

app-base

Sistema SaaS multi-tenant desarrollado en Laravel.

DOCUMENTOS DE REFERENCIA DEL PROYECTO

Usar como contexto los siguientes documentos del proyecto:

CONTEXTO-FIJO-DEL-PROYECTO – app-base
CONVENCIONES-DEL-PROYECTO – app-base
CHECKLIST OFICIAL PARA CREAR MÓDULOS NUEVOS
MAPA-DE-ENTIDADES – app-base
BASE-VISUAL-DEL-PROYECTO – app-base
TODO-DEL-PROYECTO – app-base

ARQUITECTURA DEL SISTEMA

Tipo de sistema:

Plataforma SaaS multi-tenant desarrollada en Laravel.

Aislamiento de datos mediante:

tenant_id

Traits obligatorios en entidades del negocio:

TenantScoped
ResolvesTenantRouteBinding
SoftDeletes

Fuente del tenant operativo:

app('tenant')

La arquitectura multi-tenant ya está implementada y no debe modificarse.

Convención de base de datos:

tenants.id es char(36)
tenant_id no debe asumirse como foreignId()
copiar siempre el patrón real validado del proyecto antes de crear nuevas migraciones

MODELO DEL NEGOCIO

users es entidad del sistema.

parties es la entidad base del negocio.

Nombre técnico:

parties

Nombre visible en la interfaz:

contactos

El acceso por empresa se resuelve por memberships.

users representa identidad global.
memberships representa acceso por tenant.

BASE VISUAL

Las páginas internas utilizan el layout estándar:

<x-page> <x-breadcrumb /> <x-page-header /> <x-card /> </x-page>

Patrón de listados:

page.list-page
card.list-card
table-wrap.list-scroll

Existen patrones recientes validados para vistas show:

summary-inline-grid
detail-grid
detail-grid--3
tabs internas para información secundaria
badges de estado resueltos desde catálogo en el core

Convenciones recientes de frontend:

evitar handlers JS inline en Blade
preferir data-action="app-..." para comportamiento reutilizable
resolver comportamiento reusable en app-base.js
resolver comportamiento puntual de pantalla con @push('scripts')
en vistas con tabs, preservar contexto activo cuando mejore la UX

ESTADO ACTUAL DEL PROYECTO

Arquitectura SaaS implementada.

Multi-tenancy funcional.

Middleware ResolveTenant operativo.

Traits de aislamiento funcionando.

Base visual definida.

La numeración de orders y documents se gestiona mediante el sistema de secuencias document_sequences, evitando numeración manual o restricciones unique globales.

ENTIDADES IMPLEMENTADAS

users
tenants
memberships
parties
projects
tasks
products
assets
orders
order_items
documents
document_items

REGLA DE DESARROLLO

Los nuevos módulos deben seguir el patrón utilizado en:

models
controllers
routes
vistas
estructura CRUD

Si el módulo tiene cabecera y detalle:

diseñar primero la estructura y relaciones
luego crear migraciones
luego implementar cabecera
luego implementar ítems

CATÁLOGOS FUNCIONALES

Antes de implementar campos con valores cerrados (por ejemplo status, kind o type):

verificar si el sistema ya dispone de un catálogo reutilizable
si corresponde, reutilizar o extender los catálogos existentes en:

app/Support/Catalogs

Evitar definir strings hardcodeados directamente en controladores, vistas o seeders.

REGLA DE CREACIÓN DE ENTIDADES

Antes de crear una entidad nueva seguir el procedimiento definido en:

CHECKLIST OFICIAL PARA CREAR MÓDULOS NUEVOS

OBJETIVO DE ESTE CHAT

[describir aquí el objetivo puntual del chat]


Avancemos con pasos cortos
no dudes en preguntar o solicitar información para darme la mejor respuesta
no es necesario adivinar, ya tenemos el proyecto con varios modulos
es más util pedir información así oprimizamos el uso del tiempo
siempre dame la mejor opcion de respuesta para no tener sobrerevisar
prevenimos el analisi viendo si el siguiente paso es mejor o no
Si necesitas archivos creame un comado de consola similar al siguiente:

(
find app/Models -type f -name "Product.php";
find app/Http/Controllers -type f -name "ProductController.php";
find resources/views/products -type f -name "*.blade.php";
) | sort | while read file; do
    echo "===== $file ====="
    cat "$file"
    echo
    echo
done > documentos/auditoria/auditoria-[nombre de referencia].txt

indicando todos los que necesaitas así te los paso

Si hay que crear archivo/s dame el comando de lilnea para generarlo o 
si corresponde el comando artinsan
