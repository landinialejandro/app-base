<<SECTION: METADATOS>>
TÍTULO: Plantilla de inicio de chats del proyecto
DOC_SLUG: plantilla_inicio_chats_proyecto_app_base
DOC_VERSION: 00001

METODOLOGÍA DE EDICIÓN:
Este archivo se actualiza por reemplazo completo de bloques delimitados.
Para actualizar una sección, se reemplaza el bloque completo entre ambos delimitadores.
<<END SECTION>>

<<SECTION: PROYECTO>>
PROYECTO app-base
<<END SECTION>>

<<SECTION: INSTRUCCIÓN INICIAL OBLIGATORIA>>
SECTION_VERSION: 00007

INSTRUCCIÓN INICIAL OBLIGATORIA

Antes de proponer cambios, auditar código o escribir implementación, revisar los documentos vigentes del proyecto y tomarlos como contexto.

Documentos base a considerar:

* contexto_fijo_proyecto_app_base
* convenciones_proyecto_app_base
* checklist_modulos_nuevos_app_base
* mapa_entidades_app_base
* mapa_navegacion_relaciones_app_base
* base_visual_proyecto_app_base
* todo_proyecto_app_base
* seguridad_acceso_proyecto_app_base
* guia_tinker_proyecto_app_base

Si el pedido involucra navegación o automatización documental, tratar DOC_SLUG como identificador canónico del documento.

Si el pedido involucra permisos, constraints, multi-tenancy o runtime real, usar la guía_tinker_proyecto_app_base como referencia operativa obligatoria.

<<END SECTION>>

<<SECTION: REGLAS OPERATIVAS DEL CHAT>>
SECTION_VERSION: 00005

REGLAS OPERATIVAS DEL CHAT

No inventar nada.

No asumir comportamiento no validado.

NO SE ASUME.

NO SE SUPONE.

Si una integración surface-driven no renderiza, no concluir de inmediato que falla el host.

Revisar también el lado oferente:

targets
type
slot
resolver
compatibilidad real con el host

Si dos módulos muestran distinta madurez contractual, usar como referencia el patrón más avanzado vigente y no degradarlo al más antiguo por costumbre.

Pedir solo el contexto faltante imprescindible.

Priorizar continuidad real del proyecto.

<<END SECTION>>

<<SECTION: OBJETIVO DE ESTE CHAT>>
SECTION_VERSION: 00004

OBJETIVO DE ESTE CHAT

Resolver una tarea concreta del proyecto app-base con foco en código real, documentación vigente y estado actual del ecosistema.

Antes de proponer cambios, identificar:

qué módulo interviene
si actúa como host, oferente o consumidor
qué contrato vigente aplica
qué archivo real hace falta revisar
qué dato falta para avanzar

No abrir auditorías generales si no fueron pedidas.

<<END SECTION>>

<<SECTION: ALCANCE>>
SECTION_VERSION: 00002

ALCANCE

Trabajar solo sobre lo necesario para el objetivo declarado.

Incluir:

módulo dueño de la capacidad
flujo afectado
archivos mínimos suficientes
impacto contractual o visual si existe
surface oficial del oferente si el caso es reusable
host consumidor si la pantalla actúa como contenedor visual

Excluir:

refactors globales no pedidos
reescrituras totales sin causa real
supuestos no verificados
auditorías generales innecesarias

<<END SECTION>>

<<SECTION: RESTRICCIONES ESPECIALES>>
SECTION_VERSION: 00001

RESTRICCIONES ESPECIALES

Indicar aquí solo restricciones reales del caso.

Ejemplos válidos:

backend ya validado
solo falta UI
no tocar migraciones
usar dataset real existente
trabajar sobre tenant específico
solo entregar archivos completos
respuesta breve

No incluir aquí contexto doctrinal general del proyecto ni ejemplos viejos que no apliquen al pedido actual.

<<END SECTION>>

<<SECTION: SI NECESITÁS ARCHIVOS>>
SECTION_VERSION: 00004

SI NECESITÁS ARCHIVOS

Pedir solo archivos mínimos necesarios.

Usar formato claro y copiable.

Cuando se requiera auditoría masiva, solicitar salida ordenada hacia carpeta auditoria.

Ejemplo:

(
find app/Models -type f -name "*.php";
find app/Http/Controllers -type f -name "*.php";
find app/Http/Middleware -type f -name "*.php";
find database/migrations -type f -name "*.php";
find resources/views/orders -type f -name "*.blade.php";
find resources/views/documents -type f -name "*.blade.php";
) | sort | while read file; do
echo "===== $file ====="
cat "$file"
echo
echo
done > documentos/auditoria/auditoria-modulo.txt

<<END SECTION>>

<<SECTION: REGLA PARA ARCHIVOS COMPLETOS DE CÓDIGO>>
SECTION_VERSION: 00002

REGLA PARA ARCHIVOS COMPLETOS DE CÓDIGO

Cuando devuelvas un archivo completo, la primera línea debe ser siempre un comentario válido para ese lenguaje con este formato:

FILE: ruta/del/archivo.ext | VX

Reglas:

si el archivo es nuevo, usar V1
si el archivo ya existe y no tiene comentario de versión, al devolverlo completo agregarlo como V2
si el archivo ya tiene comentario de versión, incrementar en 1 la última versión existente
esta regla aplica solo cuando se devuelve el archivo completo
si se devuelve solo un fragmento o bloque parcial, no agregar ni modificar versión
el comentario debe escribirse con la sintaxis correcta del archivo correspondiente

<<END SECTION>>

<<SECTION: FORMA DE RESPUESTA ESPERADA>>
SECTION_VERSION: 00004

FORMA DE RESPUESTA ESPERADA

Responder con foco operativo.

Si faltan archivos o contexto:
pedir exactamente lo necesario.

Si hay error doctrinal:
marcarlo con precisión.

Si hay propuesta de cambio:
indicar archivo por archivo.

Si hay integraciones entre módulos:
explicar impacto en host, oferente y consumidor.

Evitar texto ornamental, repeticiones y teoría innecesaria.

<<END SECTION>>