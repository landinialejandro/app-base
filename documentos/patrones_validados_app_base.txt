<<SECTION: METADATOS>>
SECTION_VERSION: 00001

TÍTULO: Patrones validados del proyecto
DOC_SLUG: patrones_validados_app_base
DOC_VERSION: 00001
TIPO: memoria-operativa
ESTADO: activo
GESTIÓN: Catálogo de patrones técnicos y funcionales ya aprobados en uso real.
<<END SECTION>>

<<SECTION: OBJETIVO>>
SECTION_VERSION: 00001

OBJETIVO

Registrar soluciones repetibles ya validadas para acelerar nuevas implementaciones y mantener coherencia.

Incluye:

patrones UI
patrones navegación
patrones seguridad
patrones integración
patrones runtime

<<END SECTION>>

<<SECTION: PATRONES VIGENTES>>
SECTION_VERSION: 00010

PATRONES VIGENTES

SHOW PAGE HOST

Zonas explícitas:

header_actions
summary_items
detail_items
tabs
tab_panels

NAVEGACIÓN CONTEXTUAL

trail = contexto externo
return_tab = contexto interno

SEGURIDAD

Security central
Policy/Gate alineadas
UI refleja resultado autorizado

INTEGRACIÓN MODULAR

consumer solicita
módulo oferente resuelve
componente oficial representa
host monta

TINKER REAL

usuario real
tenant real
bind tenant
Security real
dataset real

UX OPERATIVA

acciones donde nace la intención
modales para micro-operaciones
mínima fricción
sin pasos innecesarios

INDEX OPERATIVO

tabla principal dominante
métricas livianas secundarias
filtros claros
acciones visibles

MIGRACIÓN REUSABLE

cerrar capacidad completa en nueva familia
evitar convivencia eterna legacy + nueva

RECONVERSIÓN LIMPIA DE CONSTRAINTS

Cuando cambia la dimensión real de autorización, la constraint funcional debe migrar a esa dimensión y eliminar la convivencia legacy.

Caso validado:

parties:
antes → allowed_kinds sobre parties.kind
ahora → allowed_party_roles sobre party_roles.role

Regla:

la constraint anterior no debe quedar como fallback operativo si ya no representa la semántica vigente del módulo.

AUTORIZACIÓN SENSIBLE CENTRALIZADA

Cuando una regla sensible aparece duplicada en controller y requests, debe extraerse a una pieza reusable del dominio correspondiente.

Caso validado:

PartyEmployeeContactAuthorization

Ubicación vigente:

app/Support/Parties/PartyEmployeeContactAuthorization.php

Caso validado adicional:

TenantProfileAccess

Ubicación vigente:

app/Support/Tenants/TenantProfileAccess.php

Regla:

la UI puede expresar disponibilidad, pero la autorización sensible debe resolverse y bloquearse en backend.

Regla adicional:

cuando un frente tenant concentra reglas owner/admin, esa autoridad debe centralizarse en una pieza reusable del plano tenant y no repetirse en policies, controllers o Blades.

VALIDACIÓN RUNTIME DE SEGURIDAD

Una validación sensible debe combinar:

UI correcta
Security runtime
request validation
intento manipulado
verificación final de persistencia

Caso validado:

protección de party vinculada a membership activa.

Caso validado adicional:

contrato owner/admin en tenant/profile/accesses.

Validaciones realizadas:

admin intentando editar General bloqueado

admin intentando modificar permisos del rol admin bloqueado

admin modificando permisos de rol operativo permitido

admin intentando asignar admin bloqueado

admin asignando rol operativo permitido

admin intentando autogestionarse o vaciar sus roles bloqueado

admin intentando combinar admin con rol operativo vía sync bloqueado

membership activa sin roles con error funcional controlado

rollback confirmado luego de pruebas positivas

La validación no se considera cerrada solo por ocultar campos en UI.

Debe probarse que el backend bloquea intentos manipulados y que el registro persiste intacto.

EVENTOS OPERATIVOS TRANSVERSALES

Patrón validado:

módulo dueño
↓
autoriza, valida y muta
↓
emite evento operativo liviano
↓
listener queued con ShouldQueue y afterCommit
↓
prestador transversal aplica sanidad
↓
persistencia append-only

Reglas:

el evento operativo no autoriza
el evento operativo no valida la operación original
el evento operativo no reemplaza Security ni policies
el módulo dueño conserva ownership funcional
la capacidad transversal registra solo si corresponde
el listener queued no debe bloquear el flujo principal

Caso vigente:

OperationalActivity

Módulos con integración validada a OperationalActivity:

appointments
tasks
orders
documents
projects
assets
products
attachments
parties
inventory

CAMPOS OBSERVABLES POR CATÁLOGO DEL MÓDULO DUEÑO

Cuando una capacidad transversal registra actividad operativa, los campos observables deben pertenecer al catálogo del módulo dueño.

Casos vigentes:

AppointmentCatalog::activityTrackedFields()
TaskCatalog::activityTrackedFields()
OrderCatalog::activityTrackedFields()
DocumentCatalog::activityTrackedFields()

Regla:

el prestador transversal no debe conocer en exceso la semántica interna de cada módulo.

Cuando se integre un nuevo módulo, el catálogo dueño debe declarar explícitamente los campos observables antes de considerar cerrada la trazabilidad operativa.

SANIDAD DE CAMBIOS OPERATIVOS

La trazabilidad operativa debe aplicar sanidad antes de persistir.

Reglas:

no registrar cambios vacíos
ignorar campos técnicos
no registrar por registrar
registrar solo cambios operativamente significativos
clasificar actividad desde el prestador transversal cuando corresponda

Clasificaciones vigentes posibles:

assigned
reassigned
unassigned
status_changed
updated

Regla:

la clasificación no debe dispersarse en controllers.

METADATA OPERATIVA OPCIONAL

Los eventos operativos livianos pueden admitir metadata opcional cuando un cambio operativo relevante no vive directamente como atributo simple del modelo principal.

Reglas:

la metadata debe complementar el cambio operativo
no debe trasladar autorización al evento
no debe convertir entidades hijas en módulos propios
no debe acoplar en exceso OperationalActivity a la semántica interna del módulo dueño

Caso validado:

parties registra cambios de party_roles como metadata operativa de Party.

RELACIONES HIJAS CON SIGNIFICANCIA OPERATIVA

Cuando una relación hija expresa un cambio operativo relevante, puede representarse como metadata operativa del record principal si esa lectura respeta ownership y evita crear un módulo artificial.

Caso validado:

party_roles expresa relación del contacto con la empresa.

Sus cambios pueden registrarse dentro de la actividad operativa de Party.

Regla:

party_roles no se convierte en módulo propio ni en navegación propia.

ENTIDADES AGRUPADORAS Y MOVIMIENTOS ATÓMICOS

Cuando un módulo posee una entidad agrupadora de acción y entidades atómicas de trazabilidad fina, la actividad transversal debe anclarse en la pieza que represente mejor la acción operativa del usuario.

Caso validado:

inventory registra InventoryOperation.

InventoryMovement conserva trazabilidad fina interna de stock.

Regla:

no duplicar en OperationalActivity la trazabilidad atómica que ya pertenece al módulo dueño cuando una operación agrupada expresa mejor el evento operativo.

LECTURA GENERAL DE ACTIVIDAD OPERATIVA

Patrón validado:

capacidad tenant no modular
↓
Security::allows('operational_activity.viewAny')
↓
lectura tenant-scoped desde tenant/profile
↓
tab visual Actividad

Reglas:

la lectura general del log puede existir sin convertir OperationalActivity en módulo funcional ordinario

OperationalActivity no se agrega a ModuleCatalog por este patrón

la vista general inicial no crea navegación principal propia

la tab Actividad en tenant/profile es lectura visual general

Patrón de resolución de lectura:

operational_activities guarda trazabilidad estable
OperationalActivityLinkResolver resuelve presentación y navegación
Blade consume filas ya resueltas

Campos persistidos usados como base:

module
record_type
record_id

Datos derivados en lectura:

record_label
record_url
actor_label
subject_label
trail

Regla:

el link visual al registro asociado no debe persistirse como URL final.

Debe resolverse dinámicamente desde una pieza de lectura.

Si no existe ruta show canónica segura, se muestra texto plano.

Trail validado desde actividad general:

Inicio
Perfil de empresa · Actividad
Registro destino

LECTURA CONTEXTUAL HEREDADA DE ACTIVIDAD OPERATIVA

Patrón validado:

host autoriza view del record
↓
surface tenant/transversal recibe record autorizado
↓
OperationalActivityContextReader lee actividad asociada
↓
OperationalActivitySurfaceService prepara tab
↓
x-host-tabs representa Actividad

Regla:

si el usuario puede ver el registro host,
puede ver la actividad operativa asociada a ese registro.

Esta lectura no equivale a lectura general del log del tenant.

Reglas:

no utiliza operational_activity.view_any
no habilita acceso general al log
no convierte OperationalActivity en módulo funcional
no crea navegación principal
no reconstruye historia desde campos actuales del modelo
no traslada autorización al prestador transversal

Hosts validados:

tasks.show
parties.show
projects.show
assets.show
products.show
appointments.show
orders.show
documents.show

Tab validada:

key = activity

Regla:

si no hay activity_record_set ni activity_context declarado, la lectura contextual no muestra actividad de otros registros.

ACTIVITY RECORD SET MODULAR

Patrón validado:

módulo dueño
↓
declara activity_record_set en ModuleCatalog
↓
provider implementa ActivityRecordSetProvider
↓
OperationalActivitySurfaceService consulta el catálogo
↓
OperationalActivityContextReader lee actividad del conjunto
↓
tabla reusable representa filas ya resueltas

Regla:

usar activity_record_set cuando la ampliación del dataset pueda representarse como conjunto explícito de records relacionados.

Caso validado:

ProjectActivityRecordSet

Lectura validada:

Project + Tasks

Reglas:

OperationalActivity no decide la frontera agregada
el módulo dueño define el conjunto
el conjunto no autoriza records por sí mismo
el conjunto no habilita lectura general del tenant
el conjunto no altera ownership funcional de los records incluidos

ACTIVITY CONTEXT MODULAR

Patrón validado:

módulo dueño
↓
declara activity_context en ModuleCatalog
↓
provider implementa ActivityContextProvider
↓
OperationalActivitySurfaceService consulta el catálogo
↓
provider devuelve payload contextual compatible
↓
OperationalActivityContextReader lee actividad según criterio definido
↓
tabla reusable representa filas ya resueltas

Regla:

usar activity_context cuando la ampliación del dataset no pueda representarse como simple conjunto explícito de records relacionados.

Caso validado:

PartyEmployeeActivityContext

Lectura validada:

Party employee con membership activa vinculada
actividad directa de Party
actividad donde el user vinculado sea actor_user_id
actividad donde el user vinculado sea subject_user_id

Reglas:

activity_context no reemplaza activity_record_set
activity_context no autoriza records por sí mismo
activity_context no habilita lectura general del tenant
activity_context no traslada lógica interna del módulo a OperationalActivity
activity_context pertenece al módulo dueño

LECTURA VISUAL DE CAMBIOS OPERATIVOS

Patrón validado:

metadata.changes persistido
↓
OperationalActivityChangePresenter
↓
change_summary
↓
change_details
↓
operational-activity-table
↓
modal Ver cambios

Reglas:

Blade no interpreta metadata.changes directamente
la tabla muestra síntesis compacta
el detalle amplio se abre bajo demanda
el modal no decide autorización
el modal recibe contenido preparado

Caso validado:

Ver cambios dentro de tabla reusable de actividad.

ACCIONES SECUNDARIAS INFORMATIVAS EN TABLAS

Patrón validado:

acción secundaria informativa
↓
botón icon-only
↓
modal o detalle bajo demanda

Caso validado:

Ver cambios
x-icons.changes
btn btn-secondary btn-icon

Reglas:

no crear clase nueva si existe variante visual suficiente
no agregar columnas nuevas si la tabla pierde compacidad
mantener bajo protagonismo visual para acciones informativas repetidas

ICONOGRAFÍA DE SURFACES TENANT/TRANSVERSALES

Patrón validado:

surface no modular declara icon
↓
x-host-tabs representa icon
↓
la capacidad conserva naturaleza no modular

Caso validado:

OperationalActivitySurfaceService declara icon = activity para tab Actividad.

Regla:

la iconografía de una surface no convierte la capacidad en módulo funcional ni navegación principal.

EDICIÓN DE FUNCIONES DE MEMBERSHIP

Patrón validado:

tabla limpia
↓
badges de funciones actuales
↓
acción contextual por fila
↓
modal de edición
↓
JS guía selección
↓
backend sincroniza y autoriza
↓
validación runtime confirma bloqueo de manipulación

Caso validado:

tenant/profile/accesses

Elementos visuales:

columna Funciones
badges de funciones actuales
botón Editar funciones
modal con checkboxes
mensaje de ayuda dinámico
acciones Cancelar / Guardar funciones

Reglas:

la acción debe nacer donde está la intención

el modal reduce ruido visual cuando la operación es breve y contextual

JS puede guiar exclusividad visual de admin y advertencias básicas

JS no decide autoridad

backend debe recibir el conjunto deseado y validar agregados y remociones

backend debe bloquear combinaciones sensibles y autogestión indebida

no debe quedar protección sensible solo en Blade ni JS

Caso vigente:

TenantMembershipRoleController::sync

Regla:

sync es patrón válido cuando el usuario edita el conjunto completo de funciones de una membership.

SEED DEMO OPERATIVO CON TRAZABILIDAD REAL

Patrón validado:

seeder demo que representa carga operativa real
↓
crea registros válidos según catálogos vigentes
↓
respeta lifecycle real del módulo
↓
emite eventos operativos livianos cuando corresponde
↓
genera jobs queued
↓
procesa la cola
↓
confirma jobs = 0
↓
confirma failed_jobs = 0
↓
confirma persistencia en operational_activities
↓
confirma ausencia de estados inválidos y residuos legacy reales

Reglas:

no todo seeder debe emitir actividad operativa

solo corresponde cuando la carga demo representa una operación real del sistema

no reemplaza eventos operativos transversales

consume el patrón vigente de OperationalActivity

no simula UI completa

no depende de controllers, requests web, navegador ni sesión HTTP

usa código interno del sistema, modelos, servicios y eventos reales

no debe sostener compatibilidad legacy silenciosa

no debe corregir payloads antiguos por dentro

no debe crear datos que el sistema vigente ya no reconoce

debe fallar fuerte si el payload no cumple catálogos o lifecycle vigentes

Casos validados:

PartyModuleSeeder

Crea parties demo como carga operativa real.

Emite OperationalRecordCreated por cada Party nueva creada por seeder.

Incluye metadata operativa de party_roles.

Validación final informada:

22 actividades parties
jobs = 0
failed = 0

OrderModuleSeeder

Crea órdenes demo listas para operar.

Elimina compatibilidad legacy interna basada en legacyKinds.

Declara explícitamente group y kind.

Usa:

group = sale|purchase|service
kind = standard

Incluye orden testigo:

TECH-ORD-CYCLE-0001

La orden testigo recorre ciclo completo:

creación
ítem
transición a pending_approval
aprobación
ejecución de línea por inventario
cierre

La ejecución de línea usa OrderInventoryOperationService y respeta InventoryOperation como ancla operativa de inventario.

Validación final informada:

estado final closed
ítem final completed
orders = 10
inventory = 1
jobs = 0
failed = 0

DocumentModuleSeeder

Crea documentos listos para operar mediante modelos Document y DocumentItem.

Deja de usar DB::table como carga dura para documentos e ítems.

Elimina estado legacy issued.

Usa estados vigentes de DocumentCatalog.

Deja documentos en pending_approval o approved.

Deja ítems documentales en pending.

Emite actividad operativa por creación y transición.

Validación final informada:

documents = 10
jobs = 0
failed = 0

Validación complementaria informada:

orders_invalid = []
documents_invalid = []

Regla:

un seeder demo operativo no debe considerarse cerrado solo por migrate:fresh --seed OK.

Debe validarse también cola, actividad persistida, catálogos, lifecycle y ausencia de legacy real.

<<END SECTION>>